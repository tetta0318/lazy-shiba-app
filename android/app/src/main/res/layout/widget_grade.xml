<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical">

    <TextView
        android:id="@+id/widget_grade_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#000000"
        android:padding="8dp"
        android:text="今期の成績予想"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:textSize="14sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="12dp">

        <TextView
            android:id="@+id/widget_grade_attendance_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="出席率  75%"
            android:textColor="#333333"
            android:textSize="13sp" />

        <ProgressBar
            android:id="@+id/widget_grade_attendance_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="12dp"
            android:max="100"
            android:progress="75"
            android:progressTint="#D9534F" />

        <TextView
            android:id="@+id/widget_grade_overall_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="全体の成績  35%"
            android:textColor="#333333"
            android:textSize="13sp" />

        <ProgressBar
            android:id="@+id/widget_grade_overall_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:layout_marginTop="4dp"
            android:max="100"
            android:progress="35"
            android:progressTint="#D9534F" />
    </LinearLayout>

</LinearLayout>